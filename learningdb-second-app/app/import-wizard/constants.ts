/** Columns auto-injected or chosen outside WizardFields — shared across wizard and history edit. */
export const OMIT_ACTIVITY_LOG = new Set<string>(["USER_ID", "ACTIVITY_ID"]);
export const OMIT_ACTIVITY_OUTPUT = new Set<string>(["ACTI_LOG_ID"]);
export const OMIT_KIT_COUNT = new Set<string>(["AO_ID"]);
export const OMIT_KIT_READING = new Set<string>(["AO_ID"]);
